/* ============================================================
   hub/main.js — entry point. All behaviour lives in the Hub
   controller and its components; this just boots it.
   ============================================================ */

import { Hub } from './Hub.js';

Hub.boot();
